import {Stage} from "../model/entity/Stage";

export class DoComplect {
  name: string;
  stageId: number;

  constructor(name: string, stageId: number) {
    this.name = name;
    this.stageId = stageId;
  }
}
